#!/bin/sh

# Prints all batteries, their percentage remaining and an emoji corresponding
# to charge status (🔌 for plugged up, 🔋 for discharging on battery, etc.).

# Function to get the battery status icon
get_status_icon() {
    case "$1" in
    Full) echo "⚡" ;;
    Discharging) echo "🔋" ;;
    Charging) echo "🔌" ;;
    "Not charging") echo "🛑" ;;
    Unknown) echo "♻️" ;;
    *) echo "" ;;
    esac
}

# Function to print battery status
battery_status() {
    battery_path=$1
    capacity=$(cat "$battery_path/capacity" 2>/dev/null)
    status=$(cat "$battery_path/status" 2>/dev/null)
    icon=$(get_status_icon "$status")
    [ -z "$icon" ] && return
    case $(basename "$battery_path") in
    BAT?*)
        [ "$icon" = "🔋" ] && [ "$capacity" -le 25 ] && warn="❗"
        printf "%s%s%d%%" "$icon" "$warn" "$capacity"
        ;;
    hid*)
        model="$(cat "$bluebattery/model_name")"
        notify-send "$icon $model" "$capacity%"
        ;;
    esac
    unset warn
}

# Check Bluetooth batteries (only on middle-click)
bluetooth() {
    devices=$(upower -e | grep -iv 'line' | grep -iv 'display' | grep -v 'BAT[0-9]')

    for device in $devices; do
        device_name=$(upower -i "$device" | grep "model" | awk -F ': ' '{print $2}' | sed 's/ //g')
        battery_level=$(upower -i "$device" | grep "percentage" | awk -F ': ' '{print $2}' | sed 's/ //g')

        if [ -n "$battery_level" ]; then
            notify-send "🔋 $device_name:" "$battery_level"
        fi
    done
}

# Handle mouse click actions
case "$BLOCK_BUTTON" in
2) bluetooth ;; # Middle click for Bluetooth battery levels
3) notify-send "🔋 Battery module" "\- 🔋: discharging
- 🛑: not charging
- ♻️: stagnant charge
- 🔌: charging
- ⚡: fully charged
- ❗: battery very low!
- Middle click: bluetooth battery levels via upower" ;;
6) setsid -f "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

# Loop through all internal/extra batteries
for battery in /sys/class/power_supply/BAT?*; do
    battery_status "$battery"
done && printf "\\n"
