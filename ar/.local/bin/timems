#!/bin/sh

set -e

ts=$(date +%s%N)

# Execute the command passed as arguments
"$@"

# Calculate the elapsed time in milliseconds
tt=$(($(date +%s%N) - ts))
tt=$(expr "$tt" / 1000000)

# Output the time to stderr
echo "time $tt ms" >&2
